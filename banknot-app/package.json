{
  "name": "banknot-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Use dev scripts from apps manually\"",
    "build": "echo \"Use build scripts from apps manually\"",
    "lint": "eslint apps packages --ext .ts,.tsx"
  },
  "devDependencies": {}
}